/* exported getFirstChar */

// Create a new function called getFirstChar, with a single parameter, a string (called 'string'). The objective of this function is to return the first character of a string.
// Use the CharAt method to target the string at the index of 0.

function getFirstChar (string) {
  var firstCharacter = string.charAt(0);
  return firstCharacter
};
