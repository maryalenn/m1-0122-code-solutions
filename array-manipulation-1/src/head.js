/* exported head */

// Create a new function called head with a single parameter (array). The goal of this function is to return the first element of the array.

function head (array) {
  return array[0];
};
