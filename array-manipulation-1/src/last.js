/* exported last */

// Create a function with one parameter (array). The return of this function will be the last element in the array.

function last (array) {
  return array[(array.length - 1)]
};
